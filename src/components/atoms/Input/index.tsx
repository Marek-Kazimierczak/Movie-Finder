import React from "react";

import "./Input.scss";

const Input = () => {
  return (
    <input
      placeholder="Search a movie..."
      type="text"
      className="search-bar__input"
    />
  );
};

export default Input;
