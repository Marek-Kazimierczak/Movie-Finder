import React from "react";
import "./Button.scss";

const Button = () => {
  return (
    <button className="search-bar__button" type="submit">
      search
    </button>
  );
};

export default Button;
